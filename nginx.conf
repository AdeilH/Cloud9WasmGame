server {
    listen $PORT;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    # Optional: Handle WASM MIME type correctly if needed
    types {
        application/wasm wasm;
    }
}
